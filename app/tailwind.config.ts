import type { Config } from 'tailwindcss'

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        cartoon: ['Fredoka', 'sans-serif'],
      },
    },
  },
  plugins: [],
}

export default config;
